<Window x:Class="IPCim.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IPCim"
        mc:Ignorable="d"
        Title="IpcimWpf" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <DataGrid x:Name="IpGrid"
                  Grid.Column="0"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  ItemsSource="{Binding Entries}"
                  Margin="0,0,10,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="domainname" Binding="{Binding DomainName}" />
                <DataGridTextColumn Header="ipadress" Binding="{Binding IpAddress}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="10,10,0,0">
            <TextBlock Text="Domain-name:" FontSize="14" Margin="0,0,0,6" />
            <TextBox x:Name="DomainInput" Width="180" Margin="0,0,0,16" />

            <TextBlock Text="IP cím:" FontSize="14" Margin="0,0,0,6" />
            <TextBox x:Name="IpInput" Width="180" Margin="0,0,0,16" />

            <Button Content="Bevitel"
                    Width="80"
                    Margin="0,0,0,10"
                    Click="Bevitel_Click" />
            <Button Content="Mentés"
                    Width="80"
                    Click="Mentes_Click" />
        </StackPanel>
    </Grid>
</Window>
